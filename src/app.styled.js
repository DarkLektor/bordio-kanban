import styled from "styled-components";

export const Wrapper = styled.div`
  min-height: 100vh;
  width: 100%;
  display: flex;
`;
export const Main = styled.main`
  bacground: var(--white);
  width: 100%;
`;
